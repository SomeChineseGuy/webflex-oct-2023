import GetImage from "@/components/GetImage"
import Form from "@/components/Form"

const getCookie = () => {
  
}

const AboutUs = (something) => {
  console.log(something);

  return (
    <div>
      <GetImage />
      <Form />
    </div>
  )
}

export default AboutUs

// Front end
// Use anything from react
// use custom hooks
// use normal hooks